; PlatformIO project for the paddlesense ESP32 data logger.
; Board: ESP32-WROOM-32 (FCC ID 2A53N-ESP32) — supports Bluetooth Classic SPP.
;
; Build:  pio run
; Upload: pio run -t upload
; Monitor: pio device monitor

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600

; Adding the WiFi stack + HTTP server grows the image; use the 3 MB app
; partition (no OTA) so the link always fits. This device is flashed over USB.
board_build.partitions = huge_app.csv

; The ESP32 Arduino core ships BluetoothSerial, SD, Wire, Preferences and
; esp32-hal-crc (hardware CRC32) — no external libraries are required.
build_flags =
    -DCORE_DEBUG_LEVEL=1
    -Iinclude
    -std=gnu++17

build_unflags =
    -std=gnu++11
